services:
  - type: web
    name: canvas-task-manager
    runtime: node
    buildCommand: npm install && cd client && npm install && npm run build && cd .. && npm run db:init
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: TURSO_DATABASE_URL
        sync: false
      - key: TURSO_AUTH_TOKEN
        sync: false
